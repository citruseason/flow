{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "echo '[Flow] Plugin loaded. Use /harness-init to set up your project, then /meeting to start.'"
          }
        ],
        "description": "Notify user that Flow plugin is available"
      }
    ]
  }
}
