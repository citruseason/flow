{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "echo '[Flow] Brainstorming plugin loaded. Use /brainstorm to start a design session before /plan.'"
          }
        ],
        "description": "Notify user that Flow plugin is available alongside ECC"
      }
    ]
  }
}
